{
  "name": "vanilla-ts-match3",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p . --noEmit && esbuild src/main.ts --bundle --outfile=dist/main.js",
    "watch": "tsc -p . -w"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}